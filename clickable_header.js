const header = document.querySelector(`header`)

header.addEventListener(`click`, function() {
    window.location.href = `https://theeviljade.github.io/`
    header.style.cursor = `pointer`;
})
